using UnityEngine;

namespace Overdrive.Content.Tests.Fixtures
{
    /// <summary>
    /// Committed fixture ScriptableObject for story 3-8 topology tests — the track root
    /// fixture (a plain data carrier; no references needed).
    /// </summary>
    [CreateAssetMenu(fileName = "TrackFixtureData", menuName = "Overdrive Tests/Track Fixture Data")]
    public sealed class TrackFixtureData : ScriptableObject
    {
    }
}
