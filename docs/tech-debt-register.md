# Tech Debt Register

- **2026-06-24** (Story 002: Core Event Bus): ~~`once()` stub not tested~~ — RESOLVED in Story 003
- **2026-06-24** (Story 003: Edge Cases): qa-lead QL-TEST-COVERAGE reported inaccurate test counts per AC (e.g., AC-4a: said 4, actual 3) — coverage verdict correct but counts unreliable — tracked from production/epics/event-bus/story-003-edge-cases.md
- **2026-06-24** (Story 001: Core FSM): `getCurrentState()` is public with `@internal` tag — ADR-0024 Decision 6 rejects "available but discouraged" pattern. Acceptable for Story 001 (tests need it), but should be restricted or removed when Story 003 (Event Bus) lands — tracked from production/epics/game-state-machine/story-001-core-fsm-transition-table.md
- **2026-06-24** (Story 002: Async Lifecycle Hooks): `console.warn` in foundation module (line 225) — hard to intercept/redirect. Replace with Event Bus emission when Story 003 lands — tracked from production/epics/game-state-machine/story-002-async-lifecycle-hooks.md
- **2026-06-24** (Story 003: Event Bus Integration): `getCurrentState()` still public with `@internal` — Event Bus now integrated, but accessor needed for tests. Consider restricting to test-only access or removing when all consumers migrate to event subscription — tracked from production/epics/game-state-machine/story-003-event-bus-integration.md
- **2026-06-24** (Story 004: Transition Throttling): `transition()` and `_doTransition()` duplicate ~40 lines of hook/emission/rollback logic — justified by microtask boundary avoidance but maintenance hazard. Consider extracting shared `_executeTransition()` when pipeline wiring lands — tracked from production/epics/game-state-machine/story-004-transition-throttling.md
- **2026-06-25** (Story 002: Orchestrator Lifecycle): `takeSnapshot()` calls `serialize()` twice per system — once for state, once inside `hash()`. Double serialize costs ~2× at 60fps with many systems. Consider `hashState(state)` free function or cached state parameter — tracked from production/epics/simulation-snapshot/story-002-orchestrator-lifecycle.md
- **2026-06-25** (Story 006: DevGuard): QA edge cases `.call(null)` and `.bind(performance)()` not explicitly tested — inherently safe by implementation (function reference replaced entirely) but QA spec mentions them — tracked from production/epics/determinism-contract/story-006-determinism-enforcement.md
- **2026-06-25** (Story 001: Persistence State Machine + Init): Scope overshoot — save/load/delete fully implemented in Story 001 (story said "stubs"). Story 002 finds a head start — tracked from production/epics/persistence/story-001-persistence-state-machine-init.md
- **2026-06-25** (Story 002: save/load Round-trip + Key Prefix): Stale reference to `tests/integration/persistence.test.ts` in story QA section — file does not exist, tests live in `tests/unit/persistence.test.ts` — tracked from production/epics/persistence/story-002-save-load-key-prefix.md
- **2026-06-25** (Story 003: Error Isolation): Same stale reference to `tests/integration/persistence.test.ts` in story QA section (line 94) — tracked from production/epics/persistence/story-003-error-isolation.md
- **2026-06-25** (Story 004: Degraded Mode + retry()): Hardcoded queue limit `50` at persistence.ts:335 — extract to named constant `MAX_WRITE_QUEUE` — tracked from production/epics/persistence/story-004-degraded-mode-retry.md
- **2026-06-25** (Story 005: Migration Chain): `load()` triggers migration on any version mismatch (not only `stored < current` as specified in Implementation Note 8). Functionally correct (downgrade defense in `_runMigrations`) but deviates from spec — tracked from production/epics/persistence/story-005-migration-chain.md
- **2026-06-25** (Story 003: SHA-256 + Determinism): `computeSnapshotHash` concatenates JSON states without delimiters — collision risk if two different system configs produce identical concatenated strings. Acceptable for MVP (not adversarial). Add length-prefix or `\0`-separator if used for network integrity — tracked from production/epics/simulation-snapshot/story-003-sha256-determinism.md
- **2026-06-25** (Scan): DRY violation — `transition()` and `_doTransition()` share ~98 lines of hook/emission/rollback logic in `src/foundation/gsm/GameStateMachine.ts:259-304, 441-486` — extract shared `_executeTransition()` — Effort: S
- **2026-06-25** (Scan): Duplicate code — two 27-line blocks in `retry()` share similar probe+flush logic in `src/foundation/persistence/persistence.ts:691-697, 721-729` — extract shared helper — Effort: S
- **2026-06-25** (Scan): Duplicate code — two 20-line blocks share queue filter pattern in `src/foundation/persistence/persistence.ts:361-364, 488-491` — extract shared helper — Effort: S
- **2026-06-25** (Scan): Dead code — `CreateSceneGUI` and `CreateMainScene` in `src/playground/gui.ts`, `src/playground/main-scene.ts` — development scaffolding, not production code — Effort: S
- **2026-06-25** (Scan): Unused export — `ZERO` in `src/foundation/determinism/types.ts` — exported but never imported — Effort: S
- **2026-06-25** (Scan): Files over 500 lines — `persistence.ts` (738 lines), `GameStateMachine.ts` (553 lines) — consider extracting migration logic to separate module — Effort: M
- **2026-06-25** (Scan): Biome warnings — 7 non-null assertions (`!`) in pipeline-runtime, event-bus, GSM, persistence — bypass type safety — Effort: S
